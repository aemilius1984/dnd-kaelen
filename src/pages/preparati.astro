---
import BaseLayout from '@/layouts/BaseLayout.astro';
import DatiIniziali from '@/components/DatiIniziali.astro';
import PreparatiPicker from '@/islands/PreparatiPicker';
import Superficie from '@/components/ui/Superficie.astro';
import { caricaScheda } from '@/lib/carica-scheda';

const { pg, pool, sheetVersion } = await caricaScheda();
---

<BaseLayout titolo="Kaelen — Preparati" attiva="preparati">
  <DatiIniziali pg={pg} sheetVersion={sheetVersion} pool={pool} />
  <h1>Preparati</h1>
  <Superficie class="preparati-isola">
    <PreparatiPicker client:only="preact" />
  </Superficie>
  <p class="tenue"><a href="/scheda/">Torna alla scheda</a></p>
</BaseLayout>
