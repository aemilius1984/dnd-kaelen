# Modale Vitalità — piano di implementazione

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sostituire il percorso «modifica i PF dal pannello ⚡» con una modale a tutto schermo aperta dalla sezione dei punti ferita, con immissione a rotella in due tempi.

**Architecture:** Un'isola sola (`Vitalita.tsx`) porta il riepilogo in pagina e il `<dialog>`. Nessuna logica di stato nuova: tutti i gesti chiamano funzioni già esistenti in `sheet-state.ts`. La fisica della rotella la fa il browser (`scroll-snap`); l'unica cosa scritta sono due conversioni pure in `lib/rotella.ts`, provabili senza browser.

**Tech Stack:** Astro 7 statico, isole Preact (`client:only="preact"`), `@preact/signals`, vitest + jsdom, CSS a mano con i token di `tokens.css`.

**Spec:** `docs/superpowers/specs/2026-08-18-vitalita-design.md`

## Ordine di esecuzione, deciso il 2026-08-18

I task **non** si eseguono 1→6 di fila. L'audit regolamentare
(`docs/superpowers/specs/2026-08-18-regole-kaelen-chierico.md`) chiede di
implementare l'intero stato di Kaelen a 0 PF, e quella è esattamente la
superficie che il Task 5 costruisce: farlo prima significa non rifarlo dopo.

1. Task 1 → 4 (rotella, componente, isola, zona del pollice). Non toccano i
   TS morte.
2. **Lo stato a 0 PF**, come lavoro a sé: campo `statoVitale` in
   `StatoSessione`, `SCHEMA_VERSION` da 2 a 3 con migrazione, morte
   istantanea, e `segnaTsMorte` che passa a prendere il d20 grezzo perché
   1 e 20 naturali non sono un successo e un fallimento come gli altri.
   Comporta un aggiornamento meccanico delle chiamate in
   `PannelloAzioni.tsx` — non un ridisegno, ma tocca un file che questo
   piano dichiara fuori perimetro.
3. Task 5, costruito sul modello nuovo.
4. Task 6.

## Global Constraints

- Tutta la prosa visibile e i commenti sono **in italiano**; i soggetti dei commit in inglese.
- `client:only="preact"` per ogni isola: nessun render lato server, quindi nessuna guardia sul DOM nei componenti.
- Le isole **non contengono mai contenuto statico**.
- I valori derivati si calcolano in `src/lib/derive.ts`, non si scrivono nei dati.
- Ogni bersaglio da toccare è alto almeno **44px**.
- Verifica visiva sempre a **390×844**.
- Prima di dichiarare finito: `npm run gate` (check + build + test). Il gate costruisce **prima** di testare, perché i test di pagina leggono `dist/`.
- Il pannello `PannelloAzioni.tsx` **non si tocca** in questo piano.
- Ogni task finisce con un commit.

---

### Task 1: le due conversioni della rotella

**Files:**

- Create: `src/lib/rotella.ts`
- Test: `src/lib/__tests__/rotella.test.ts`

**Interfaces:**

- Consumes: niente.
- Produces: `PASSO: number` (40), `MINIMO: number` (0), `MASSIMO: number` (30), `valoreDaScorrimento(scrollTop: number, passo: number, minimo: number, massimo: number): number`, `scorrimentoDaValore(valore: number, passo: number, minimo: number, massimo: number): number`.

- [ ] **Step 1: Scrivi il test che fallisce**

```ts
import { describe, expect, it } from 'vitest';
import { MASSIMO, MINIMO, PASSO, scorrimentoDaValore, valoreDaScorrimento } from '@/lib/rotella';

describe('dalla posizione al numero', () => {
  it('ogni passo di scorrimento vale una cifra', () => {
    expect(valoreDaScorrimento(0, PASSO, MINIMO, MASSIMO)).toBe(0);
    expect(valoreDaScorrimento(PASSO, PASSO, MINIMO, MASSIMO)).toBe(1);
    expect(valoreDaScorrimento(PASSO * 7, PASSO, MINIMO, MASSIMO)).toBe(7);
  });

  it('a metà passo si aggancia alla cifra più vicina', () => {
    // `scroll-snap` porta sempre a un aggancio, ma la lettura avviene *anche*
    // mentre il dito trascina: lì la posizione è qualunque cosa.
    expect(valoreDaScorrimento(PASSO * 3 + PASSO * 0.4, PASSO, MINIMO, MASSIMO)).toBe(3);
    expect(valoreDaScorrimento(PASSO * 3 + PASSO * 0.6, PASSO, MINIMO, MASSIMO)).toBe(4);
  });

  it('una posizione negativa da rimbalzo elastico vale il minimo', () => {
    // Su iOS lo scorrimento oltre il bordo dà valori negativi: non sono una
    // cifra sotto lo zero, sono il dito che tira contro il fermo.
    expect(valoreDaScorrimento(-120, PASSO, MINIMO, MASSIMO)).toBe(MINIMO);
  });

  it('oltre l’ultima cifra si ferma al massimo', () => {
    expect(valoreDaScorrimento(PASSO * 999, PASSO, MINIMO, MASSIMO)).toBe(MASSIMO);
  });
});

describe('dal numero alla posizione', () => {
  it('riporta ogni cifra sotto la banda di scelta', () => {
    expect(scorrimentoDaValore(0, PASSO, MINIMO, MASSIMO)).toBe(0);
    expect(scorrimentoDaValore(5, PASSO, MINIMO, MASSIMO)).toBe(PASSO * 5);
  });

  it('è l’inverso esatto dell’andata, per ogni cifra dell’intervallo', () => {
    // Serve perché la rotella si riposiziona da sola: all'apertura, e ogni
    // volta che il campo «digita» impone un numero. Se andata e ritorno non
    // combaciano, la rotella scatta di una cifra a ogni riapertura.
    for (let n = MINIMO; n <= MASSIMO; n++) {
      const dove = scorrimentoDaValore(n, PASSO, MINIMO, MASSIMO);
      expect(valoreDaScorrimento(dove, PASSO, MINIMO, MASSIMO)).toBe(n);
    }
  });

  it('un valore fuori intervallo viene riportato dentro', () => {
    expect(scorrimentoDaValore(-4, PASSO, MINIMO, MASSIMO)).toBe(0);
    expect(scorrimentoDaValore(500, PASSO, MINIMO, MASSIMO)).toBe(PASSO * MASSIMO);
  });
});

describe('la guardia sul passo', () => {
  it('un passo nullo o negativo è un errore di programmazione, non un caso', () => {
    // Dividere per zero darebbe Infinity, che diventerebbe un numero di PF
    // e finirebbe salvato in localStorage.
    expect(() => valoreDaScorrimento(80, 0, MINIMO, MASSIMO)).toThrow();
    expect(() => scorrimentoDaValore(2, -8, MINIMO, MASSIMO)).toThrow();
  });
});
```

- [ ] **Step 2: Lancia il test e verifica che fallisca**

Run: `npx vitest run src/lib/__tests__/rotella.test.ts`
Expected: FAIL — `Failed to resolve import "@/lib/rotella"`.

- [ ] **Step 3: Scrivi l'implementazione minima**

```ts
/** La rotella dei numeri: si gira col dito e si ferma su una cifra.
 *
 *  Il movimento non è scritto da nessuna parte. Un contenitore che scorre con
 *  `scroll-snap-type: y mandatory` e una cifra per aggancio *è* una rotella:
 *  inerzia, rimbalzo e frenata li mette il browser, ed è la stessa fisica del
 *  selettore di data di iOS. Qui c'è solo la conversione fra la posizione di
 *  scorrimento e il numero sotto la banda di scelta — e siccome è aritmetica
 *  pura, si prova con vitest invece che con un browser, che questo progetto
 *  non ha. */

/** Altezza di una cifra, in px. Deve combaciare con l'altezza dichiarata
 *  nel CSS della pista: se le due divergono, la rotella si ferma fra due
 *  cifre. */
export const PASSO = 40;

export const MINIMO = 0;

/** Oltre trenta la rotella diventerebbe una corsa lunga: da lì in su si usa
 *  il campo «digita», che è comunque la strada da tastiera. */
export const MASSIMO = 30;

function guardia(passo: number): void {
  if (!(passo > 0)) {
    throw new Error(`Il passo di una rotella deve essere positivo, ricevuto ${passo}`);
  }
}

function limita(n: number, minimo: number, massimo: number): number {
  return Math.min(massimo, Math.max(minimo, n));
}

/** Il numero che sta sotto la banda di scelta a questa posizione. */
export function valoreDaScorrimento(
  scrollTop: number,
  passo: number,
  minimo: number,
  massimo: number,
): number {
  guardia(passo);
  return limita(minimo + Math.round(scrollTop / passo), minimo, massimo);
}

/** Dove va portata la pista perché quel numero finisca sotto la banda. */
export function scorrimentoDaValore(
  valore: number,
  passo: number,
  minimo: number,
  massimo: number,
): number {
  guardia(passo);
  return (limita(valore, minimo, massimo) - minimo) * passo;
}
```

- [ ] **Step 4: Lancia il test e verifica che passi**

Run: `npx vitest run src/lib/__tests__/rotella.test.ts`
Expected: PASS, 8 test.

- [ ] **Step 5: Commit**

```bash
git add src/lib/rotella.ts src/lib/__tests__/rotella.test.ts
git commit -m "feat: add the pure conversions behind the number wheel"
```

---

### Task 2: il componente della rotella

**Files:**

- Create: `src/islands/Rotella.tsx`
- Test: `src/islands/__tests__/Rotella.test.ts`

**Interfaces:**

- Consumes: `PASSO`, `MINIMO`, `MASSIMO`, `valoreDaScorrimento`, `scorrimentoDaValore` da `@/lib/rotella`.
- Produces: `export default function Rotella(props: { valore: number; onCambia: (n: number) => void })` — renderizza `<div class="rotella">` con dentro `<div class="pista" role="spinbutton" tabIndex={0}>`, una `<div class="cifra">` per ogni numero da `MINIMO` a `MASSIMO`.

- [ ] **Step 1: Scrivi il test che fallisce**

```ts
// @vitest-environment jsdom
import { afterEach, beforeEach, expect, it } from 'vitest';
import { h, render } from 'preact';
import Rotella from '@/islands/Rotella';
import { MASSIMO, MINIMO, PASSO } from '@/lib/rotella';

let radice: HTMLDivElement;
let letto: number[];

const giro = () => new Promise((r) => setTimeout(r, 50));
const pista = () => radice.querySelector<HTMLElement>('.pista')!;

const monta = (valore: number) => {
  letto = [];
  render(h(Rotella, { valore, onCambia: (n: number) => letto.push(n) }), radice);
};

beforeEach(() => {
  radice = document.createElement('div');
  document.body.append(radice);
});

afterEach(() => {
  render(null, radice);
  radice.remove();
});

it('offre una cifra per ogni numero dell’intervallo', async () => {
  monta(4);
  await giro();

  expect(radice.querySelectorAll('.cifra')).toHaveLength(MASSIMO - MINIMO + 1);
});

it('si annuncia come selettore di numero, non come lista', async () => {
  // Chi usa un lettore di schermo non può girare niente: senza questo ruolo
  // sentirebbe trentuno numeri sciolti e nessun valore corrente.
  monta(4);
  await giro();

  expect(pista().getAttribute('role')).toBe('spinbutton');
  expect(pista().getAttribute('aria-valuenow')).toBe('4');
  expect(pista().getAttribute('aria-valuemin')).toBe(String(MINIMO));
  expect(pista().getAttribute('aria-valuemax')).toBe(String(MASSIMO));
});

it('scorrere la pista riporta il numero sotto la banda', async () => {
  monta(4);
  await giro();

  pista().scrollTop = PASSO * 9;
  pista().dispatchEvent(new Event('scroll'));
  await giro();

  expect(letto).toContain(9);
});

it('le frecce girano la rotella di una cifra per volta', async () => {
  // La strada da tastiera. Senza, la rotella è un comando che esiste solo
  // per chi ha un dito.
  monta(4);
  await giro();

  pista().dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowUp', bubbles: true }));
  await giro();
  expect(letto).toContain(5);

  pista().dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowDown', bubbles: true }));
  await giro();
  expect(letto).toContain(3);
});

it('non scavalca gli estremi', async () => {
  monta(MINIMO);
  await giro();

  pista().dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowDown', bubbles: true }));
  await giro();

  expect(letto).not.toContain(MINIMO - 1);
});

it('non richiama onCambia se il numero non è cambiato', async () => {
  // `scrollTop` scritto da noi fa scattare un evento `scroll`: senza questa
  // guardia il componente si richiama da solo a ogni riposizionamento.
  monta(4);
  await giro();
  const prima = letto.length;

  pista().scrollTop = PASSO * 4;
  pista().dispatchEvent(new Event('scroll'));
  await giro();

  expect(letto).toHaveLength(prima);
});
```

- [ ] **Step 2: Lancia il test e verifica che fallisca**

Run: `npx vitest run src/islands/__tests__/Rotella.test.ts`
Expected: FAIL — `Failed to resolve import "@/islands/Rotella"`.

- [ ] **Step 3: Scrivi l'implementazione minima**

```tsx
import { useEffect, useRef } from 'preact/hooks';
import { MASSIMO, MINIMO, PASSO, scorrimentoDaValore, valoreDaScorrimento } from '@/lib/rotella';

interface Props {
  valore: number;
  onCambia: (n: number) => void;
}

const CIFRE = Array.from({ length: MASSIMO - MINIMO + 1 }, (_, i) => MINIMO + i);

/** La colonna di numeri che si gira col dito. Non sa cosa sia un punto ferita:
 *  produce una quantità e la consegna a chi l'ha montata. */
export default function Rotella({ valore, onCambia }: Props) {
  const pista = useRef<HTMLDivElement>(null);
  // Chi ha scritto per ultimo la posizione. Scrivere `scrollTop` fa scattare
  // un evento `scroll`, che senza questa memoria rimbalzerebbe indietro come
  // se fosse stato l'utente a girare.
  const atteso = useRef(valore);

  useEffect(() => {
    const nodo = pista.current;
    if (!nodo) return;
    atteso.current = valore;
    nodo.scrollTop = scorrimentoDaValore(valore, PASSO, MINIMO, MASSIMO);
  }, [valore]);

  const leggi = () => {
    const nodo = pista.current;
    if (!nodo) return;
    const n = valoreDaScorrimento(nodo.scrollTop, PASSO, MINIMO, MASSIMO);
    if (n === atteso.current) return;
    atteso.current = n;
    onCambia(n);
  };

  const tasto = (e: KeyboardEvent) => {
    const passi: Record<string, number> = { ArrowUp: 1, ArrowDown: -1 };
    const verso = passi[e.key];
    if (verso === undefined) return;
    e.preventDefault();
    const n = Math.min(MASSIMO, Math.max(MINIMO, valore + verso));
    if (n === valore) return;
    atteso.current = n;
    onCambia(n);
  };

  return (
    <div class="rotella">
      <div
        class="pista"
        ref={pista}
        role="spinbutton"
        tabIndex={0}
        aria-label="Quantità"
        aria-valuenow={valore}
        aria-valuemin={MINIMO}
        aria-valuemax={MASSIMO}
        onScroll={leggi}
        onKeyDown={tasto}
      >
        {CIFRE.map((n) => (
          <div class={n === valore ? 'cifra scelta' : 'cifra'} key={n}>
            {n}
          </div>
        ))}
      </div>
      <div class="banda" aria-hidden="true"></div>
      <div class="sfumatura" aria-hidden="true"></div>
    </div>
  );
}
```

- [ ] **Step 4: Lancia il test e verifica che passi**

Run: `npx vitest run src/islands/__tests__/Rotella.test.ts`
Expected: PASS, 6 test.

- [ ] **Step 5: Commit**

```bash
git add src/islands/Rotella.tsx src/islands/__tests__/Rotella.test.ts
git commit -m "feat: add the number wheel, turned by finger or by arrow keys"
```

---

### Task 3: l'isola Vitalità — riepilogo in pagina e guscio della modale

**Files:**

- Create: `src/islands/Vitalita.tsx`
- Test: `src/islands/__tests__/Vitalita.test.ts`

**Interfaces:**

- Consumes: `assicuraInizializzato`, `datiIniziali`, `stato` da `@/lib/storage`.
- Produces: `export default function Vitalita()` — renderizza `<button class="vitalita-scheda">` (il riepilogo che apre) e `<dialog class="vitalita">`.

**Nota per chi implementa:** in jsdom `HTMLDialogElement.prototype.showModal` non esiste. Il test lo definisce da sé nel `beforeEach`, come mostrato.

- [ ] **Step 1: Scrivi il test che fallisce**

```ts
// @vitest-environment jsdom
import { afterEach, beforeEach, expect, it } from 'vitest';
import { h, render } from 'preact';
import Vitalita from '@/islands/Vitalita';
import { caricaPersonaggioDaFile } from '@/lib/carica-personaggio';
import { applicaDanno, impostaPfTemporanei, statoIniziale } from '@/lib/sheet-state';
import { muta } from '@/lib/storage';

const pg = caricaPersonaggioDaFile();

let radice: HTMLDivElement;
const giro = () => new Promise((r) => setTimeout(r, 50));
const scheda = () => radice.querySelector<HTMLElement>('.vitalita-scheda')!;
const finestra = () => radice.querySelector<HTMLDialogElement>('dialog.vitalita')!;

beforeEach(async () => {
  localStorage.clear();
  // jsdom non implementa il dialogo modale: qui serve solo sapere che
  // qualcuno l'ha aperto, non vederlo aperto.
  HTMLDialogElement.prototype.showModal = function () {
    this.open = true;
  };
  HTMLDialogElement.prototype.close = function () {
    this.open = false;
  };
  document.body.innerHTML =
    `<script type="application/json" id="dati-iniziali">` +
    JSON.stringify({ pg, sheetVersion: 'v-test', pool: [] }) +
    `</script>`;
  radice = document.createElement('div');
  document.body.append(radice);
  render(h(Vitalita, {}), radice);
  // `stato` è un signal di modulo e `assicuraInizializzato` gira una volta
  // sola: senza questo ogni prova eredita i PF della precedente.
  muta(() => statoIniziale(pg, 'v-test'));
  await giro();
});

afterEach(() => {
  render(null, radice);
});

it('il riepilogo mostra i PF correnti sul massimo', async () => {
  expect(scheda().textContent).toContain(String(pg.pfMax));
});

it('il riepilogo è un bersaglio solo, non una manciata di righe', async () => {
  // Tutta la scheda apre: al tavolo si colpisce il blocco, non un link.
  expect(scheda().tagName).toBe('BUTTON');
});

it('toccare il riepilogo apre la modale', async () => {
  expect(finestra().open).toBe(false);

  scheda().click();
  await giro();

  expect(finestra().open).toBe(true);
});

it('il riepilogo segue lo stato quando cambia da fuori', async () => {
  muta((x) => applicaDanno(x, 5));
  await giro();

  expect(scheda().textContent).toContain(String(pg.pfMax - 5));
});

it('il chip dei temporanei tiene il suo posto anche a zero', async () => {
  // L'altezza della scheda è fissa e riservata: se il chip sparisse dal
  // flusso quando i temporanei sono zero, la riga si accorcerebbe e il
  // contenuto sotto salterebbe.
  const aZero = radice.querySelector('.vitalita-temp')!;
  expect(aZero).not.toBeNull();

  muta((x) => impostaPfTemporanei(x, 4));
  await giro();

  expect(radice.querySelector('.vitalita-temp')!.textContent).toContain('4');
});
```

- [ ] **Step 2: Lancia il test e verifica che fallisca**

Run: `npx vitest run src/islands/__tests__/Vitalita.test.ts`
Expected: FAIL — `Failed to resolve import "@/islands/Vitalita"`.

- [ ] **Step 3: Scrivi l'implementazione minima**

```tsx
import { useRef } from 'preact/hooks';
import { assicuraInizializzato, datiIniziali, stato } from '@/lib/storage';

/** La vitalità di Kaelen: il riepilogo che sta in pagina e la modale che
 *  apre. Un'isola sola per entrambi, così c'è un solo posto che legge i PF —
 *  prima della riscrittura il tracker e il pannello ⚡ ne avevano due, già
 *  divergenti. */
export default function Vitalita() {
  // `client:only="preact"`: nessun pre-render lato server.
  assicuraInizializzato();
  const { pg } = datiIniziali();
  const s = stato.value;
  const finestra = useRef<HTMLDialogElement>(null);

  const inPericolo = s.pf === 0;
  const percentuale = pg.pfMax > 0 ? Math.min(100, Math.round((s.pf / pg.pfMax) * 100)) : 0;

  return (
    <>
      <button
        type="button"
        class="vitalita-scheda"
        aria-label="Punti ferita, apri la vitalità"
        onClick={() => finestra.current?.showModal()}
      >
        <span class="testata">
          <span class="kicker">punti ferita</span>
          <span class="apri">apri</span>
        </span>

        <span class="numero">
          <span class={inPericolo ? 'pf pericolo' : 'pf'}>{s.pf}</span>
          <span class="su">/ {pg.pfMax}</span>
          {/* Occupa il suo posto anche a zero: l'altezza è fissa e riservata. */}
          <span class="vitalita-temp" hidden={s.pfTemporanei === 0}>
            +{s.pfTemporanei} temp
          </span>
        </span>

        <span class="metro">
          <span class="riempimento" style={{ width: `${percentuale}%` }}></span>
        </span>
        <span class="tacche" aria-hidden="true"></span>

        <span class="piede">
          <span class="dadi">
            dadi {pg.numeroDadiVita - s.dadiVitaSpesi}/{pg.numeroDadiVita}
          </span>
          <span class={s.ispirazione ? 'isp accesa' : 'isp'}>isp</span>
        </span>
      </button>

      <dialog class="vitalita" ref={finestra} aria-label="Vitalità">
        <div class="testa">
          <span class="kicker">Vitalità</span>
          <button type="button" aria-label="Chiudi" onClick={() => finestra.current?.close()}>
            ×
          </button>
        </div>
      </dialog>
    </>
  );
}
```

- [ ] **Step 4: Lancia il test e verifica che passi**

Run: `npx vitest run src/islands/__tests__/Vitalita.test.ts`
Expected: PASS, 5 test.

- [ ] **Step 5: Commit**

```bash
git add src/islands/Vitalita.tsx src/islands/__tests__/Vitalita.test.ts
git commit -m "feat: make the hit point block a single target that opens a modal"
```

---

### Task 4: la zona del pollice — rotella e i tre verbi

**Files:**

- Modify: `src/islands/Vitalita.tsx`
- Test: `src/islands/__tests__/Vitalita.test.ts` (aggiungi al file del Task 3)

**Interfaces:**

- Consumes: `Rotella` dal Task 2; `applicaCura`, `applicaDanno`, `impostaPfTemporanei` da `@/lib/sheet-state`; `muta` da `@/lib/storage`.
- Produces: dentro il dialogo, `.zona-pollice` con `<Rotella>`, `button.verbo-danno`, `button.verbo-cura`, `button.verbo-temp`, e `input.digita` (`type="number"`).

- [ ] **Step 1: Scrivi il test che fallisce**

```ts
const verbo = (nome: string) => radice.querySelector<HTMLButtonElement>(`.verbo-${nome}`)!;
const digita = () => radice.querySelector<HTMLInputElement>('.digita')!;

const scegli = async (n: number) => {
  digita().value = String(n);
  digita().dispatchEvent(new Event('input', { bubbles: true }));
  await giro();
};

it('il danno toglie esattamente la quantità scelta', async () => {
  await scegli(5);
  verbo('danno').click();
  await giro();

  expect(scheda().textContent).toContain(String(pg.pfMax - 5));
});

it('la cura non porta oltre il massimo', async () => {
  muta((x) => applicaDanno(x, 3));
  await scegli(30);
  verbo('cura').click();
  await giro();

  expect(scheda().textContent).toContain(String(pg.pfMax));
});

it('i temporanei si impostano, non si sommano', async () => {
  await scegli(7);
  verbo('temp').click();
  await giro();
  await scegli(2);
  verbo('temp').click();
  await giro();

  expect(radice.querySelector('.vitalita-temp')!.textContent).toContain('2');
});

it('ogni verbo dice cosa farà con la quantità corrente', async () => {
  // Due tempi: prima il numero, poi il verbo. Se il verbo non ripete il
  // numero, il secondo tempo si fa alla cieca.
  await scegli(6);

  expect(verbo('danno').textContent).toContain('6');
  expect(verbo('cura').textContent).toContain('6');
});

it('l’esito finisce in una regione annunciata', async () => {
  // Chi non vede il numero cambiare deve sentire che è successo qualcosa.
  await scegli(4);
  verbo('danno').click();
  await giro();

  const annuncio = radice.querySelector('[aria-live]')!;
  expect(annuncio.textContent).toContain(String(pg.pfMax - 4));
});
```

- [ ] **Step 2: Lancia il test e verifica che fallisca**

Run: `npx vitest run src/islands/__tests__/Vitalita.test.ts`
Expected: FAIL — `.verbo-danno` è `null`.

- [ ] **Step 3: Scrivi l'implementazione minima**

Aggiungi in cima al file gli import:

```tsx
import { useRef, useState } from 'preact/hooks';
import Rotella from '@/islands/Rotella';
import { applicaCura, applicaDanno, impostaPfTemporanei } from '@/lib/sheet-state';
import { assicuraInizializzato, datiIniziali, muta, stato } from '@/lib/storage';
import { MASSIMO, MINIMO } from '@/lib/rotella';
```

Dentro il componente, prima del `return`:

```tsx
const [quanto, setQuanto] = useState(1);
const [annuncio, setAnnuncio] = useState('');

const applica = (fn: (x: typeof s) => typeof s, quanti: number, verbo: string) => {
  muta(fn);
  setAnnuncio(`${verbo} ${quanti}. ${stato.value.pf} punti ferita.`);
};
```

E dentro `<dialog>`, dopo `.testa`:

```tsx
        <div class="zona-pollice">
          <div class="quanto">
            <span class="kicker">quanto, e poi cosa</span>
            <Rotella valore={quanto} onCambia={setQuanto} />
            <label class="riga-digita">
              <span class="kicker">digita</span>
              <input
                class="digita"
                type="number"
                min={MINIMO}
                inputMode="numeric"
                value={quanto}
                onInput={(e) => {
                  const grezzo = e.currentTarget.value;
                  // Un campo vuoto non è «zero»: è una cifra a metà. Finché
                  // non arriva un numero, la quantità non si tocca.
                  if (grezzo === '') return;
                  setQuanto(Math.max(MINIMO, Number(grezzo)));
                }}
              />
            </label>
          </div>

          <div class="verbi">
            <button
              type="button"
              class="verbo-danno"
              onClick={() => applica((x) => applicaDanno(x, quanto), quanto, 'Danno')}
            >
              <span class="nome">Danno</span>
              <span class="effetto">toglie {quanto} PF</span>
            </button>
            <button
              type="button"
              class="verbo-cura"
              onClick={() => applica((x) => applicaCura(x, pg, quanto), quanto, 'Cura')}
            >
              <span class="nome">Cura</span>
              <span class="effetto">rimette {quanto} PF</span>
            </button>
            <button
              type="button"
              class="verbo-temp"
              onClick={() => applica((x) => impostaPfTemporanei(x, quanto), quanto, 'Temporanei')}
            >
              <span class="nome">Temporanei</span>
              <span class="effetto">imposta a {quanto}</span>
            </button>
          </div>
        </div>

        <p class="annuncio" aria-live="polite">
          {annuncio}
        </p>
```

Nota: la rotella accetta fino a `MASSIMO`; il campo `digita` no, ed è il motivo per cui esiste.

- [ ] **Step 4: Lancia il test e verifica che passi**

Run: `npx vitest run src/islands/__tests__/Vitalita.test.ts`
Expected: PASS, 10 test.

- [ ] **Step 5: Commit**

```bash
git add src/islands/Vitalita.tsx src/islands/__tests__/Vitalita.test.ts
git commit -m "feat: pick the amount, then the verb"
```

---

### Task 5: le righe del consumabile — dadi vita, Ispirazione, TS morte

**Files:**

- Modify: `src/islands/Vitalita.tsx`
- Test: `src/islands/__tests__/Vitalita.test.ts` (aggiungi al file)

**Interfaces:**

- Consumes: `impostaIspirazione`, `segnaTsMorte`, `spendiDadoVitaConCura` da `@/lib/sheet-state`.
- Produces: `.riga-dadi button`, `.riga-isp button`, `.riga-ts` con `button.ts-successo` e `button.ts-fallimento`.

- [ ] **Step 1: Scrivi il test che fallisce**

```ts
const comando = (riga: string) => radice.querySelector<HTMLButtonElement>(`.riga-${riga} button`)!;

it('spendere un dado vita lo scala e cura almeno un PF', async () => {
  muta((x) => applicaDanno(x, 6));
  await giro();
  await scegli(4);

  comando('dadi').click();
  await giro();

  expect(scheda().textContent).toContain(`${pg.numeroDadiVita - 1}/${pg.numeroDadiVita}`);
  expect(scheda().textContent).toContain(String(pg.pfMax - 2));
});

it('l’Ispirazione si prende quando è spenta e si spende quando è accesa', async () => {
  // Era una stella da guardare: in una modale che serve ad agire, una riga
  // di sola lettura è un buco.
  expect(comando('isp').textContent).toMatch(/prendi/i);

  comando('isp').click();
  await giro();
  expect(comando('isp').textContent).toMatch(/spendi/i);

  comando('isp').click();
  await giro();
  expect(comando('isp').textContent).toMatch(/prendi/i);
});

it('i TS contro morte compaiono solo a zero PF', async () => {
  expect(radice.querySelector('.riga-ts')).toBeNull();

  muta((x) => applicaDanno(x, pg.pfMax));
  await giro();

  expect(radice.querySelector('.riga-ts')).not.toBeNull();
});

it('i TS contro morte si segnano e si contano', async () => {
  muta((x) => applicaDanno(x, pg.pfMax));
  await giro();

  radice.querySelector<HTMLButtonElement>('.ts-successo')!.click();
  await giro();
  radice.querySelector<HTMLButtonElement>('.ts-fallimento')!.click();
  await giro();

  const riga = radice.querySelector('.riga-ts')!;
  expect(riga.textContent).toContain('1');
});
```

- [ ] **Step 2: Lancia il test e verifica che fallisca**

Run: `npx vitest run src/islands/__tests__/Vitalita.test.ts`
Expected: FAIL — `.riga-dadi button` è `null`.

- [ ] **Step 3: Scrivi l'implementazione minima**

Aggiungi agli import di `sheet-state`: `impostaIspirazione`, `segnaTsMorte`, `spendiDadoVitaConCura`.

Dentro `<dialog>`, fra `.testa` e `.zona-pollice`:

```tsx
<div class="righe">
  <div class="riga riga-dadi">
    <span class="kicker">dadi vita</span>
    <span class="conto">
      {pg.numeroDadiVita - s.dadiVitaSpesi}/{pg.numeroDadiVita} ({pg.dadoVita})
    </span>
    <button
      type="button"
      disabled={s.dadiVitaSpesi >= pg.numeroDadiVita}
      onClick={() => applica((x) => spendiDadoVitaConCura(x, pg, quanto), quanto, 'Dado vita')}
    >
      Spendi
    </button>
  </div>

  <div class="riga riga-isp">
    <span class="kicker">ispirazione eroica</span>
    <span class={s.ispirazione ? 'stella accesa' : 'stella'} aria-hidden="true">
      {s.ispirazione ? '★' : '☆'}
    </span>
    <button
      type="button"
      onClick={() =>
        applica(
          (x) => impostaIspirazione(x, !s.ispirazione),
          0,
          s.ispirazione ? 'Ispirazione spesa' : 'Ispirazione presa',
        )
      }
    >
      {s.ispirazione ? 'Spendi' : 'Prendi'}
    </button>
  </div>

  {inPericolo && (
    <div class="riga riga-ts">
      <span class="kicker">ts contro morte</span>
      <span class="conto">
        {s.tsMorte.successi} ✓ · {s.tsMorte.fallimenti} ✗
      </span>
      <span class="coppia">
        <button
          type="button"
          class="ts-successo"
          aria-label="Segna un successo"
          onClick={() => applica((x) => segnaTsMorte(x, 'successo'), 0, 'Successo')}
        >
          ✓
        </button>
        <button
          type="button"
          class="ts-fallimento"
          aria-label="Segna un fallimento"
          onClick={() => applica((x) => segnaTsMorte(x, 'fallimento'), 0, 'Fallimento')}
        >
          ✗
        </button>
      </span>
    </div>
  )}
</div>
```

Nota: `spendiDadoVitaConCura` prende i PF curati dal tavolo — il sito non tira dadi — e la regola garantisce almeno 1 PF. Qui il totale arriva dalla rotella, che è il numero che il giocatore ha appena tirato.

- [ ] **Step 4: Lancia il test e verifica che passi**

Run: `npx vitest run src/islands/__tests__/Vitalita.test.ts`
Expected: PASS, 14 test.

- [ ] **Step 5: Commit**

```bash
git add src/islands/Vitalita.tsx src/islands/__tests__/Vitalita.test.ts
git commit -m "feat: give every consumable row its own command"
```

---

### Task 6: la pagina, il CSS e la guardia dell'altezza fissa

**Files:**

- Modify: `src/pages/scheda.astro:13-19` (import) e `:74-88` (sezione PF e fascia difese)
- Delete: `src/islands/PfTracker.tsx`, `src/islands/Ispirazione.tsx`
- Modify: `src/styles/componenti.css` (`.pf-pannello`, `.pf *`, `.difese`, `.ispirazione` → nuove regole)
- Create: `src/styles/__tests__/altezze.test.ts`
- Test: `src/components/__tests__/menu.test.ts` non va toccato; il test di pagina esistente è `src/pages/__tests__/scheda.test.ts`

**Interfaces:**

- Consumes: `Vitalita` dal Task 3.
- Produces: `.vitalita-scheda { height: 210px }` composta dalle variabili `--v-testata`, `--v-numero`, `--v-metro`, `--v-tacche`, `--v-piede`, `--v-imbottitura`, `--v-bordi`.

- [ ] **Step 1: Scrivi il test che fallisce**

```ts
import { readFileSync } from 'node:fs';
import { expect, it } from 'vitest';

const CSS = readFileSync('src/styles/componenti.css', 'utf8');

/** Il corpo della prima regola che apre con `selettore {`. */
function corpo(selettore: string): string {
  const apertura = CSS.indexOf(`${selettore} {`);
  if (apertura === -1) throw new Error(`regola non trovata: ${selettore}`);
  const chiusura = CSS.indexOf('}', apertura);
  return CSS.slice(apertura, chiusura);
}

it('l’altezza riservata della vitalità è la somma delle sue parti', () => {
  // Le cinque altezze riservate di questo progetto sono numeri *misurati* con
  // Chrome headless, e una è già segnalata come incerta perché nessuna
  // sessione recente ha avuto un browser. Questa no: è una somma di costanti
  // dichiarate, e questo test la fa tornare. Se qualcuno cambia una riga
  // senza aggiornare il totale, fallisce il gate invece di far saltare la
  // pagina alla prima idratazione.
  const blocco = corpo('.vitalita-scheda');

  const parti = [...blocco.matchAll(/--v-[a-z]+:\s*(\d+)px/g)].map((m) => Number(m[1]));
  const totale = /height:\s*(\d+)px/.exec(blocco);

  expect(parti.length).toBe(7);
  if (!totale) throw new Error('altezza fissa non dichiarata');
  expect(parti.reduce((a, b) => a + b, 0)).toBe(Number(totale[1]));
});

it('ogni parte è usata davvero dal layout, non solo dichiarata', () => {
  // Una variabile che nessuno consuma è un numero che mente: la somma
  // tornerebbe mentre il layout fa altro.
  for (const nome of ['testata', 'numero', 'metro', 'tacche', 'piede']) {
    expect(CSS).toContain(`var(--v-${nome})`);
  }
});

it('i bersagli della modale non scendono sotto i 44px', () => {
  // Il minimo per un dito. Le righe sono 56, i verbi 76, i comandi 40 dentro
  // righe da 56 — che è il bersaglio vero.
  expect(corpo('dialog.vitalita .riga')).toMatch(/height:\s*56px/);
  expect(corpo('dialog.vitalita .verbi button')).toMatch(/height:\s*76px/);
});
```

- [ ] **Step 2: Lancia il test e verifica che fallisca**

Run: `npx vitest run src/styles/__tests__/altezze.test.ts`
Expected: FAIL — `regola non trovata: .vitalita-scheda`.

- [ ] **Step 3: Scrivi l'implementazione minima**

In `src/styles/componenti.css`, sostituisci il blocco `.pf-pannello` / `.pf …` con:

```css
/* L'altezza della scheda della vitalità è FISSA, non minima, e non è
 * misurata: è la somma delle sette costanti qui sotto. Le altre isole di
 * questo progetto riservano il proprio spazio con numeri presi da Chrome
 * headless, e una di quelle è già segnalata come incerta perché la sessione
 * che l'ha scritta non aveva un browser. Questa non si aggiunge alla lista —
 * `src/styles/__tests__/altezze.test.ts` fa tornare i conti a ogni gate.
 *
 * È anche il motivo per cui il chip dei temporanei occupa il suo posto anche
 * a zero: se sparisse dal flusso, la riga si accorcerebbe. */
.vitalita-scheda {
  --v-testata: 20px;
  --v-numero: 48px;
  --v-metro: 25px;
  --v-tacche: 16px;
  --v-piede: 51px;
  --v-imbottitura: 48px; /* 2 × var(--spazio-3) */
  --v-bordi: 2px;
  height: 210px;

  display: block;
  width: 100%;
  margin: var(--spazio-2) 0;
  padding: var(--spazio-3);
  text-align: left;
  cursor: pointer;
  border-radius: var(--raggio-superficie);
  background: var(--superficie);
  border: 1px solid var(--filetto);
  box-shadow: var(--ombra-2);
  color: var(--inchiostro);
}
.vitalita-scheda .testata {
  height: var(--v-testata);
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.vitalita-scheda .apri {
  font-size: 0.66rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--inchiostro-muto);
}
.vitalita-scheda .numero {
  height: var(--v-numero);
  display: flex;
  align-items: flex-end;
  gap: 0.5rem;
}
.vitalita-scheda .pf {
  font-family: var(--font-mono);
  font-size: 3rem;
  line-height: 0.95;
  color: var(--lampo);
}
.vitalita-scheda .pf.pericolo {
  color: var(--allerta);
}
.vitalita-scheda .su {
  font-family: var(--font-mono);
  font-size: 1rem;
  line-height: 2.4;
  color: var(--inchiostro-tenue);
}
/* `hidden` toglierebbe il chip dal flusso e accorcerebbe la riga: qui deve
 * sparire alla vista e tenere il posto. */
.vitalita-scheda .vitalita-temp {
  margin-left: auto;
  padding: 0.25rem 0.5rem;
  border: 1px solid var(--ambra);
  border-radius: 999px;
  color: var(--ambra);
  font-family: var(--font-mono);
  font-size: 0.82rem;
}
.vitalita-scheda .vitalita-temp[hidden] {
  display: block;
  visibility: hidden;
}
.vitalita-scheda .metro {
  height: var(--v-metro);
  display: block;
  margin-top: 0.7rem;
  border: 1px solid var(--filetto);
  border-radius: 3px;
  background: var(--superficie-incassata);
  overflow: hidden;
}
.vitalita-scheda .metro .riempimento {
  display: block;
  height: 12px;
  background: var(--lampo);
}
/* Due gradienti sovrapposti invece di ventidue nodi: quello fitto disegna
 * le tacche corte ogni 16px, quello rado le lunghe ogni quinta. Il primo si
 * ferma a 7px di altezza, il secondo arriva a 12. */
.vitalita-scheda .tacche {
  height: var(--v-tacche);
  display: block;
  margin-top: 4px;
  background-repeat: repeat-x;
  background-position:
    0 0,
    0 0;
  background-size:
    16px 7px,
    80px 12px;
  background-image:
    repeating-linear-gradient(to right, var(--inchiostro) 0 1px, transparent 1px 16px),
    repeating-linear-gradient(to right, var(--inchiostro) 0 1px, transparent 1px 80px);
  opacity: 0.3;
}
.vitalita-scheda .piede {
  height: var(--v-piede);
  display: flex;
  align-items: center;
  gap: 1.1rem;
  margin-top: 0.9rem;
  padding-top: 0.8rem;
  border-top: 1px solid var(--filetto);
  font-size: 0.66rem;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--inchiostro-muto);
}
.vitalita-scheda .isp.accesa {
  color: var(--ambra);
}

/* La modale: a tutto schermo come l'archivio, non una tendina dal basso. Si
 * apre per agire con calma su una cosa sola, e la zona del pollice deve
 * poter stare in fondo. */
dialog.vitalita {
  width: 100%;
  max-width: none;
  height: 100%;
  max-height: none;
  margin: 0;
  padding: var(--spazio-2) var(--spazio-2) calc(1.2rem + env(safe-area-inset-bottom));
  border: none;
  background: var(--carta);
  color: var(--inchiostro);
  display: flex;
  flex-direction: column;
}
dialog.vitalita::backdrop {
  background: rgb(0 0 0 / 0.6);
}
dialog.vitalita .riga {
  height: 56px;
  display: flex;
  align-items: center;
  gap: 0.6rem;
  border-top: 1px solid var(--filetto);
}
dialog.vitalita .riga button {
  margin-left: auto;
  height: 40px;
  padding: 0 0.85rem;
  background: transparent;
  border: 1px solid var(--filetto);
  border-radius: var(--raggio);
  color: var(--lampo);
  font-size: 0.68rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}
dialog.vitalita .zona-pollice {
  margin-top: auto;
  display: flex;
  gap: var(--spazio-2);
  align-items: stretch;
}
dialog.vitalita .verbi {
  flex: 1 1 0;
  display: flex;
  flex-direction: column;
  gap: 0.6rem;
}
dialog.vitalita .verbi button {
  height: 76px;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: center;
  gap: 0.15rem;
  padding: 0 1rem;
  background: var(--superficie);
  border: 1px solid var(--filetto);
  border-radius: var(--raggio);
}
dialog.vitalita .verbo-danno {
  border-color: var(--allerta);
  color: var(--allerta);
}
dialog.vitalita .verbo-cura {
  border-color: var(--lampo);
  color: var(--lampo);
}
dialog.vitalita .verbo-temp {
  border-color: var(--ambra);
  color: var(--ambra);
}
dialog.vitalita .verbi .nome {
  font-size: 0.72rem;
  font-weight: 600;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}
dialog.vitalita .verbi .effetto {
  font-size: 0.74rem;
  color: var(--inchiostro-muto);
}
/* La rotella: l'aggancio e l'inerzia li fa il browser. */
.rotella {
  position: relative;
  width: 108px;
  height: 252px;
  border: 1px solid var(--filetto);
  border-radius: var(--raggio);
  background: var(--superficie-incassata);
  overflow: hidden;
}
.rotella .pista {
  height: 100%;
  overflow-y: auto;
  scroll-snap-type: y mandatory;
  scrollbar-width: none;
  padding-block: 106px; /* (252 − 40) / 2, così la prima e l'ultima cifra arrivano sotto la banda */
}
.rotella .pista::-webkit-scrollbar {
  display: none;
}
.rotella .cifra {
  height: 40px;
  line-height: 40px;
  text-align: center;
  scroll-snap-align: center;
  font-family: var(--font-mono);
  font-size: 1.35rem;
  color: var(--inchiostro-tenue);
  opacity: 0.55;
}
.rotella .cifra.scelta {
  font-size: 2.3rem;
  color: var(--inchiostro);
  opacity: 1;
}
.rotella .banda {
  /* Deve combaciare *esattamente* con una cella: 40px come `.cifra`, e in
   * mezzo alla pista come il suo `padding-block`. Con una banda più alta
   * della cella la rotella sembra fermarsi fra due cifre. */
  position: absolute;
  left: 0;
  right: 0;
  top: 106px;
  height: 40px;
  border-top: 1px solid var(--lampo);
  border-bottom: 1px solid var(--lampo);
  pointer-events: none;
}
.rotella .sfumatura {
  position: absolute;
  inset: 0;
  pointer-events: none;
  background: linear-gradient(
    180deg,
    var(--superficie-incassata) 0%,
    rgb(228 218 196 / 0) 26%,
    rgb(228 218 196 / 0) 74%,
    var(--superficie-incassata) 100%
  );
}
dialog.vitalita .annuncio {
  position: absolute;
  width: 1px;
  height: 1px;
  overflow: hidden;
  clip-path: inset(50%);
}
```

Nella stessa `componenti.css`, porta `.difese` a tre colonne e togli `.ispirazione`:

```css
.difese {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.25rem;
  text-align: center;
  margin: var(--spazio-2) 0;
  padding: 0.5rem 0;
  border-block: 1px solid var(--filetto);
}
```

In `src/pages/scheda.astro`, sostituisci gli import di `PfTracker` e `Ispirazione` con `import Vitalita from '@/islands/Vitalita';`, e il blocco delle righe 74-88 con:

```astro
<Vitalita client:only="preact" />

<div class="difese">
  <div><span class="tenue">CA</span><span class="valore">{ca}</span></div>
  <div><span class="tenue">CD</span><span class="valore">{cd}</span></div>
  <div><span class="tenue">INIZ</span><span class="valore">{bonusIniziativa}</span></div>
</div>
```

Nota: `Vitalita` non va più avvolta in `<Superficie>` — la scheda porta da sé fondo, filetto, raggio e ombra, perché la sua altezza dev'essere una somma di costanti e `Superficie` aggiungerebbe imbottitura non contata.

Infine cancella i due file sostituiti:

```bash
git rm src/islands/PfTracker.tsx src/islands/Ispirazione.tsx
```

- [ ] **Step 4: Lancia il gate intero e verifica che passi**

Run: `npm run gate`
Expected: check senza errori, build completata, tutti i test verdi.

Se `src/pages/__tests__/scheda.test.ts` fallisce su `class="pannello"`, la causa è che hai lasciato una `Superficie` intorno a `Vitalita`: toglila, non aggirare il test.

- [ ] **Step 5: Commit**

```bash
git add -A src/pages/scheda.astro src/styles/componenti.css src/styles/__tests__/altezze.test.ts src/islands
git commit -m "feat: land the Vitalità modal on the sheet"
```

---

## Cosa resta aperto dopo l'ultimo commit

Le tre verifiche che nessuna sessione senza browser può chiudere, già elencate
nella spec: che la rotella giri bene col dito e che `scrollend` arrivi quando
serve su iOS; che le tacche del metro non si impastino a 390px; che i 210px
di altezza fissa reggano alla resa vera. Vanno guardate a 390×844 prima del
rilascio su `main`.
