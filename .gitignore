node_modules/
dist/
.astro/
.wrangler/
.DS_Store
# segreti: i file veri mai, gli esempi sì. `.dev.vars` è il nome che il
# runtime di Cloudflare legge quando le Pages Functions girano in locale, e
# senza questa riga un file con la password vera dentro finirebbe committato.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example
# icone e service worker generati a build-time
public/icon-192.png
public/icon-512.png
public/apple-touch-icon.png
public/icon.svg

# scratch di processo (ledger, brief, report, diff di review)
.superpowers/

# font self-hosted generati a build-time
public/fonts/
